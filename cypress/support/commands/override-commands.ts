/**
 * @file Custom overrides of default Cypress commands
 * @description Override of existing command in the Cypress API.
 * @example <caption>Dismiss element if found</caption>
 * `Cypress.Commands.overwrite("visit", (originalFn, url, options) => { ... })`
 * @see https://on.cypress.io/custom-commands#Overwrite-Existing-Commands
 */

{
  "Modal": {
    "ariaLabelledby": {
      "type": "string",
      "defaultValue": null,
      "required": true,
      "externalProp": false,
      "description": "Accessible title for the Modal."
    },
    "isOpen": {
      "type": "boolean",
      "defaultValue": null,
      "required": true,
      "externalProp": false,
      "description": "Determins the state of the Modal."
    },
    "onDismiss": {
      "type": "VoidFunction",
      "defaultValue": null,
      "required": true,
      "externalProp": false,
      "description": "Function to call when whenever the user hits \"Escape\" or clicks outside the dialog. It's important to close the Modal `onDismiss`."
    },
    "size": {
      "type": "Sizes",
      "defaultValue": null,
      "required": true,
      "externalProp": false,
      "description": "Control whether the Modal is default width or wide."
    },
    "allowPinchZoom": {
      "type": "boolean",
      "defaultValue": true,
      "required": false,
      "externalProp": false,
      "description": "Handle zoom/pinch gestures on iOS devices when scroll locking is enabled."
    },
    "element": {
      "type": "any",
      "defaultValue": "'MODAL'",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider"
    },
    "initialFocusRef": {
      "type": "RefObject<any>",
      "defaultValue": null,
      "required": false,
      "externalProp": false,
      "description": "By default the first focusable element will receive focus when the Modal opens but you can provide a ref to focus instead."
    }
  },
  "ModalHeader": {
    "element": {
      "type": "any",
      "defaultValue": "'MODAL_HEADER'",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider"
    },
    "i18nDismissLabel": {
      "type": "string",
      "defaultValue": "Close modal",
      "required": false,
      "externalProp": false,
      "description": "Accesssible text for the close button."
    }
  },
  "ModalHeading": {
    "as": {
      "type": "\"h1\" | \"h2\" | \"h3\" | \"h4\" | \"h5\" | \"h6\"",
      "defaultValue": "'h2'",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider"
    },
    "element": {
      "type": "any",
      "defaultValue": "'MODAL_HEADING'",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider"
    }
  },
  "ModalBody": {
    "element": {
      "type": "any",
      "defaultValue": "'MODAL_BODY'",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider"
    }
  },
  "ModalFooter": {
    "element": {
      "type": "any",
      "defaultValue": "'MODAL_FOOTER'",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider"
    }
  },
  "ModalFooterActions": {
    "element": {
      "type": "any",
      "defaultValue": "'MODAL_FOOTER_ACTIONS'",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider."
    },
    "justify": {
      "type": "Justify",
      "defaultValue": null,
      "required": false,
      "externalProp": false
    }
  }
}

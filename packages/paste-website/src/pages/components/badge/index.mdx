---
title: Badge - Components
description: A badge is a visually highlighted text label that describes an object.
slug: /components/badge/
---

import {graphql} from 'gatsby';
import Changelog from '@twilio-paste/badge/CHANGELOG.md';

import {Badge} from '@twilio-paste/badge';
import {Card} from '@twilio-paste/card';
import {Text} from '@twilio-paste/text';
import {Heading} from '@twilio-paste/heading';
import {Radio, RadioGroup} from '@twilio-paste/radio-group';
import {InformationIcon} from '@twilio-paste/icons/esm/InformationIcon';
import {WarningIcon} from '@twilio-paste/icons/esm/WarningIcon';
import {SuccessIcon} from '@twilio-paste/icons/esm/SuccessIcon';
import {ErrorIcon} from '@twilio-paste/icons/esm/ErrorIcon';
import {NewIcon} from '@twilio-paste/icons/esm/NewIcon';
import {ProductVoiceIcon} from '@twilio-paste/icons/esm/ProductVoiceIcon';
import {ProductElasticSIPTrunkingIcon} from '@twilio-paste/icons/esm/ProductElasticSIPTrunkingIcon';
import {Grid, Column} from '@twilio-paste/grid';

import {Truncate} from '@twilio-paste/truncate';
import {styled, css} from '@twilio-paste/styling-library';

import {Box} from '@twilio-paste/box';
import {UnorderedList, ListItem} from '@twilio-paste/list';
import {Stack} from '@twilio-paste/stack';
import {Paragraph} from '@twilio-paste/paragraph';
import {Table, THead, TBody, Td, Th, Tr} from '@twilio-paste/table';
import {useUID} from '@twilio-paste/uid-library';

import {SidebarCategoryRoutes} from '../../../constants';
import {DoDont, Do, Dont} from '../../../components/DoDont';
import {Callout, CalloutTitle, CalloutText} from '../../../components/callout';
import {tableExample, betaFeatureExample, settingsAndProducts} from '../../../component-examples/BadgeExamples';

export const pageQuery = graphql`
  {
    allPasteComponent(filter: {name: {eq: "@twilio-paste/badge"}}) {
      edges {
        node {
          name
          description
          status
          version
        }
      }
    }
    mdx(fields: {slug: {eq: "/components/badge/"}}) {
      fileAbsolutePath
      frontmatter {
        slug
        title
      }
      headings {
        depth
        value
      }
    }
    allAirtable(filter: {data: {Feature: {eq: "Badge"}}}) {
      edges {
        node {
          data {
            Documentation
            Figma
            Design_committee_review
            Engineer_committee_review
            Code
            status
          }
        }
      }
    }
  }
`;

<ComponentHeader
  name="Badge"
  categoryRoute={SidebarCategoryRoutes.COMPONENTS}
  githubUrl="https://github.com/twilio-labs/paste/tree/main/packages/paste-core/components/badge"
  storybookUrl="/?path=/story/components-badge--all-badges"
  data={props.data.allPasteComponent.edges}
  packageStatus={props.data.allAirtable.edges}
/>

---

<contentwrapper>

<PageAside data={props.data.mdx} />

<content>

## Guidelines

### About Badge

A badge is a small chunk of information that describes an object in a visually highlighted way. You can think of badges as a 1-3 word description of a more complex object that is highlighted to draw visual focus. Badges should be used to describe static attributes. Badges should be used to highlight static attributes of an object, for example to identify a feature that is available in a `beta` release phase. For dynamic attributes, such as the status of a dynamic process, refer to the [Status Pattern](https://paste.twilio.design/roadmap 'See our roadmap for the Status Pattern release date').

## Examples

### Default badge

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space80">
  <Badge as="span" variant="default">
    Default
  </Badge>
  <Badge as="span" variant="default">
    <InformationIcon decorative />
    Default
  </Badge>
  <Badge as="span" variant="default">
    Default
    <InformationIcon decorative />
  </Badge>
</Box>
`.trim()}
</LivePreview>

### Info badge

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space80">
  <Badge as="span" variant="info">
    Info
  </Badge>
  <Badge as="span" variant="info">
    <InformationIcon decorative />
    Info
  </Badge>
  <Badge as="span" variant="info">
    Info
    <InformationIcon decorative />
  </Badge>
</Box>
`.trim()}
</LivePreview>

### Warning badge

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space80">
  <Badge variant="warning">
    Warning
  </Badge>
  <Badge variant="warning">
    <WarningIcon decorative />
    Warning
  </Badge>
  <Badge variant="warning">
    Warning
    <WarningIcon decorative />
  </Badge>
</Box>
`.trim()}
</LivePreview>

### Error badge

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space80">
  <Badge variant="error">
    Error
  </Badge>
  <Badge variant="error">
    <ErrorIcon decorative />
    Error
  </Badge>
  <Badge variant="error">
    Error
    <ErrorIcon decorative />
  </Badge>
</Box>
`.trim()}
</LivePreview>

### Success badge

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space80">
  <Badge variant="success">
    Success
  </Badge>
  <Badge variant="success">
    <SuccessIcon decorative />
    Success
  </Badge>
  <Badge variant="success">
    Success
    <SuccessIcon decorative />
  </Badge>
</Box>
`.trim()}
</LivePreview>

### New badge

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space80">
  <Badge variant="new">
    New
  </Badge>
  <Badge variant="new">
    <NewIcon decorative />
    New
  </Badge>
  <Badge variant="new">
    New
    <NewIcon decorative />
  </Badge>
</Box>
`.trim()}
</LivePreview>

### Badge with anchor functionality

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space40" rowGap="space60" flexWrap="wrap">
  <Badge as="a" href="#" variant="default">
    Default
  </Badge>
  <Badge as="a" href="#" variant="info">
    Info
  </Badge>
  <Badge as="a" href="#" variant="warning">
    Warning
  </Badge>
  <Badge as="a" href="#" variant="error">
    Error
  </Badge>
  <Badge as="a" href="#" variant="success">
    Success
  </Badge>
  <Badge as="a" href="#" variant="new">
    New
  </Badge>
</Box>
`.trim()}
</LivePreview>

### Badge with button functionality

<LivePreview scope={{Box, Badge, InformationIcon, ErrorIcon, WarningIcon, SuccessIcon, NewIcon}} language="jsx">
  {`<Box display="flex" columnGap="space40" rowGap="space60" flexWrap="wrap">
  <Badge as="button" onClick={() => null} variant="default">
    Default
  </Badge>
  <Badge as="button" onClick={() => null} variant="info">
    Info
  </Badge>
  <Badge as="button" onClick={() => null} variant="warning">
    Warning
  </Badge>
  <Badge as="button" onClick={() => null} variant="error">
    Error
  </Badge>
  <Badge as="button" onClick={() => null} variant="success">
    Success
  </Badge>
  <Badge as="button" onClick={() => null} variant="new">
    New
  </Badge>
</Box>
`.trim()}
</LivePreview>

## When to use a badge

Use a badge when you want to visually highlight an attribute of an item.

### Use cases

#### Call attention to attributes of table items

<LivePreview
  scope={{
    Box,
    Table,
    THead,
    Td,
    Th,
    Tr,
    Text,
    TBody,
    Badge,
  }}
  language="jsx"
>
  {tableExample.trim()}
</LivePreview>

#### Identify an item as as a beta release

<LivePreview
  scope={{
    Box,
    Badge,
    Card,
    NewIcon,
    Heading,
    Paragraph,
  }}
  language="jsx"
>
  {betaFeatureExample.trim()}
</LivePreview>

#### Highlight products

<LivePreview
  scope={{
    Box,
    Badge,
    Grid,
    Column,
    RadioGroup,
    Radio,
    ProductElasticSIPTrunkingIcon,
    ProductVoiceIcon,
  }}
  language="jsx"
  noInline
>
  {settingsAndProducts.trim()}
</LivePreview>

### Do & Don't

<DoDont>
  <Do title="Do" body="Use 1-3 words as badge text.">
    <Badge variant="new" as="span">
      <NewIcon title="New badge component" size="sizeIcon10" />
      New Component
    </Badge>
  </Do>
  <Dont title="Don't" body="Don't use lengthy copy inside a badge.">
    <Badge variant="error" as="span">
      This is some very long text inside a badge that creates a very large badge.
    </Badge>
  </Dont>
</DoDont>

<DoDont>
  <Do title="Do" body="Set the badge children to be inline.">
    <Badge variant="new" as="span">
      Inline badge
    </Badge>
  </Do>
  <Dont title="Don't" body="Don't use multiline badge text.">
    <Badge variant="new" as="span">
      This
      <Box as="br" /> is <Box as="br" />
      not ideal
    </Badge>
  </Dont>
</DoDont>

---

## Usage Guide

### API

#### Installation

```bash
yarn add @twilio-paste/badge - or - yarn add @twilio-paste/core
```

#### Usage

```jsx
import {Badge} from '@twilio-paste/badge';

const BadgeExample = () => (
  <Badge variant="default" as="span">
    Default Badge
  </Badge>
);
```

#### Props

| Prop     | Type                                   | Description                                                        | Default     |
| -------- | -------------------------------------- | ------------------------------------------------------------------ | ----------- |
| children | `BadgeChildren`                        |                                                                    | `null`      |
| variant  | `BadgeVariants`                        | `default`, `info`, `success`, `warning`, `error`, `new`            | `null`      |
| as       | `BadgeBaseElements`                    | The HTML tag to use as base. -`span`, `button`, `a`                | `null`      |
| href?    | `string`                               | A URL to route to. Must use `as="a"` for this prop to work.        | `undefined` |
| onClick? | `MouseEventHandler<HTMLButtonElement>` | React event handler. Must use `as="button"` for this prop to work. | `undefined` |

<ChangelogRevealer>
  <Changelog />
</ChangelogRevealer>

</content>

</contentwrapper>

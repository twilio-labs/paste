import * as React from 'react';
import {Box} from '@twilio-paste/box';

export const GetStartedCardIllustration: React.FC<React.PropsWithChildren<unknown>> = ({children}) => {
  return <Box marginBottom="space70">{children}</Box>;
};

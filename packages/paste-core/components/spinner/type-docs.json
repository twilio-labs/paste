{
  "Spinner": {
    "decorative": {
      "type": "boolean",
      "defaultValue": null,
      "required": true,
      "externalProp": false,
      "description": "Whether or not the SVG is just visual flair or adds meaning to the page. Specifically for screenreaders to know whether to read out the title or not."
    },
    "delay": {
      "type": "number",
      "defaultValue": null,
      "required": false,
      "externalProp": false,
      "description": "Time delay in milliseconds of the animation."
    },
    "element": {
      "type": "any",
      "defaultValue": "SPINNER",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider"
    },
    "title": {
      "type": "string",
      "defaultValue": null,
      "required": false,
      "externalProp": false,
      "description": "The accessibility text that is read when screenreaders get to this component."
    }
  }
}

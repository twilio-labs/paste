export const meta = {
  title: '404',
  description: 'Error page',
  slug: '/404/',
};

import {Error404} from '../../components/empty-state/Error404';
import DefaultLayout from '../../layouts/DefaultLayout';
import {getNavigationData} from '../../utils/api';

export default DefaultLayout;

export const getStaticProps = async () => {
  const navigationData = await getNavigationData();
  return {
    props: {
      navigationData,
    },
  };
};

<Error404
  componentList={props.navigationData.allPasteComponent}
  layoutList={props.navigationData.allPasteLayout}
  primitiveList={props.navigationData.allPastePrimitive}
/>

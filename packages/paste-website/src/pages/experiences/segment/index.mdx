---
title: Segment
description: An overview of the Segment experience's design guidelines.
slug: /experiences/segment/
---

import {graphql} from 'gatsby';
import {WatchThisSpace} from '../../../components/empty-state/WatchThisSpace';

export const pageQuery = graphql`
  {
    mdx(frontmatter: {slug: {eq: "/experiences/segment/"}}) {
      fileAbsolutePath
      frontmatter {
        slug
        title
      }
      headings {
        depth
        value
      }
    }
  }
`;

<content>

<ComponentHeader name={props.pageContext.frontmatter.title} description={props.pageContext.frontmatter.description} />

</content>

---

<contentwrapper>

<PageAside data={props.data.mdx} />

<content>

<WatchThisSpace />

</content>
</contentwrapper>

---
title: Icons List
description: Paste comes with icons.
slug: /components/icons/
---

import {graphql} from 'gatsby';
import {IconsList} from '../../../components/icons-list';
import {P} from '../../../components/Typography';

export const pageQuery = graphql`
  {
    mdx(frontmatter: {slug: {eq: "/components/icons/"}}) {
      fileAbsolutePath
      frontmatter {
        slug
        title
      }
    }
  }
`;

<contentwrapper>
<content>

<h1>{props.pageContext.frontmatter.title}</h1>

<P variant="lead">UI, Twilio Product, and Logo icons that are available in the Paste icon system.</P>

When using any of these icons be sure to follow the [usage guidelines](/components/icons/usage-guidelines) carefully and don't forget to read the [manual installation](/introduction/for-engineers/manual-installation) page to ensure you have the correct dependencies when adding icons to your application.

</content>
</contentwrapper>

<IconsList />

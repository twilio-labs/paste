import * as React from 'react-16';
import {Box} from '@twilio-paste/box';

export const GetStartedCardIllustration: React.FC = ({children}) => {
  return <Box marginBottom="space70">{children}</Box>;
};

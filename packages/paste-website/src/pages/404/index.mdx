---
title: 404
description: Error page
slug: /404/
---

import {Error404} from '../../components/empty-state/Error404';

<Error404 componentList={[]} layoutList={[]} primitiveList={[]} />

export const pageQuery = `
  {
    allPasteComponent {
      nodes {
        name
        status
      }
    }
    allPasteLayout {
      nodes {
        name
        status
      }
    }
    allPastePrimitive {
      nodes {
        name
        status
      }
    }
  }
`;

{
  "Topbar": {
    "id": {
      "type": "string",
      "defaultValue": null,
      "required": true,
      "externalProp": false,
      "description": "Create a unique ID for the Topbar and provide it to the Sidebar component for accessible skip links"
    },
    "element": {
      "type": "any",
      "defaultValue": "TOPBAR",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider."
    }
  },
  "TopbarActions": {
    "display": {
      "type": "any",
      "defaultValue": null,
      "required": false,
      "externalProp": false,
      "description": "Used to control the display of the actions, either flex or none."
    },
    "element": {
      "type": "any",
      "defaultValue": "TOPBAR_ACTIONS",
      "required": false,
      "externalProp": false,
      "description": "Overrides the default element name to apply unique styles with the Customization Provider."
    },
    "justify": {
      "type": "Justify",
      "defaultValue": "end",
      "required": false,
      "externalProp": false,
      "description": "Used to control where the actions are aligned visually, to the start or end or center of the container."
    }
  }
}

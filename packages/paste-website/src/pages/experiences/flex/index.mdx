---
title: Flex
description: Guidelines for designing and building Flex experiences.
slug: /experiences/flex/
---

import {graphql} from 'gatsby';
import {GatsbyImage} from 'gatsby-plugin-image';
import {SidebarCategoryRoutes} from '../../../constants';
// import {WatchThisSpace} from '../../../components/empty-state/WatchThisSpace';

export const pageQuery = graphql`
  {
    mdx(frontmatter: {slug: {eq: "/experiences/flex/"}}) {
      fileAbsolutePath
      frontmatter {
        slug
        title
      }
      headings {
        depth
        value
      }
    }
  }
`;

<content>

<ComponentHeader
  name={props.pageContext.frontmatter.title}
  description={props.pageContext.frontmatter.description}
  categoryRoute={SidebarCategoryRoutes.EXPERIENCES}
/>

</content>

---

<contentwrapper>

<content>

<GatsbyImage
  src="../../../assets/images/patterns/empty-awaiting-data.png"
  layout="fullWidth"
  alt=""
  placeholder="blurred"
/>

<WatchThisSpace></WatchThisSpace>

</content>
</contentwrapper>

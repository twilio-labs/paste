---
title: Flex
description: An overview of the Flex experience's design guidelines.
slug: /experiences/flex/
---

import {graphql} from 'gatsby';
import {Card} from '@twilio-paste/card';
import {Heading} from '@twilio-paste/heading';
import {Paragraph} from '@twilio-paste/paragraph';
import {Button} from '@twilio-paste/button';
import {Anchor} from '@twilio-paste/anchor';
import {Flex} from '@twilio-paste/flex';

import {WatchThisSpace} from '../../../components/empty-state/WatchThisSpace';

export const pageQuery = graphql`
  {
    mdx(frontmatter: {slug: {eq: "/experiences/flex/"}}) {
      fileAbsolutePath
      frontmatter {
        slug
        title
      }
      headings {
        depth
        value
      }
    }
  }
`;

<content>

<ComponentHeader name={props.pageContext.frontmatter.title} description={props.pageContext.frontmatter.description} />

</content>

---

<contentwrapper>

<PageAside data={props.data.mdx} />

<content>

<WatchThisSpace />

</content>
</contentwrapper>

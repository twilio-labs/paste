name: Publish

on:
  push:
    branches:
      - main

jobs:
  components:
    name: Publish components and Storybook
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

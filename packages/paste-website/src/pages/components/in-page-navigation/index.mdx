export const meta = {
  title: 'In Page Navigation',
  package: '@twilio-paste/in-page-navigation',
  description: 'An In Page Navigation is a group of styled links that lets users navigate between related pages.',
  slug: '/components/in-page-navigation/',
};

import {Anchor} from '@twilio-paste/anchor';
import Changelog from '@twilio-paste/in-page-navigation/CHANGELOG.md';
import {InPageNavigation, InPageNavigationItem} from '@twilio-paste/in-page-navigation';
import {SidebarCategoryRoutes} from '../../../constants';
import {basicExample, fullWidthExample} from '../../../component-examples/InPageNavigationExamples';
import packageJson from '@twilio-paste/in-page-navigation/package.json';
import DefaultLayout from '../../../layouts/DefaultLayout';
import {getFeature, getNavigationData} from '../../../utils/api';

export default DefaultLayout;

export const getStaticProps = async () => {
  const navigationData = await getNavigationData();
  const feature = await getFeature('In Page Navigation');
  return {
    props: {
      data: {
        ...packageJson,
        ...feature,
      },
      navigationData,
    },
  };
};

<NormalizedComponentHeader
  categoryRoute={SidebarCategoryRoutes.COMPONENTS}
  githubUrl="https://github.com/twilio-labs/paste/tree/main/packages/paste-core/components/in-page-navigation"
  storybookUrl="/?path=/story/components-in-page-navigation--default"
  data={props.data}
/>

---

<contentwrapper>

<PageAside data={mdxHeadings} />

<content>

<LivePreview
  scope={{
    InPageNavigation,
    InPageNavigationItem,
  }}
  noInline
>
  {basicExample}
</LivePreview>

## Guidelines

### About In Page Navigation

The In Page Navigation component allows users to navigate to different pages using a styled group of links. Each In Page Navigation Item is an anchor that links to a related page. Each page within the In Page Navigation should also have an In Page Navigation component so that users can easily navigate back and forth within a set of related pages.

### In Page Navigation vs Tabs

[Tabs](/components/tabs) are used to layer related pieces of information together and display one layer at a time on the same URL. Use Tabs to alternate between views within the same context. In Page Navigation is a collection of anchors, rather than buttons. Use In Page Navigation to switch between different, related pages. Tabs replace the entire view based on the selected tab. In Page Navigation links navigate the user to a new page.

### Accessibility

- Each In Page Navigation must have a unique label. To add the label, add the `aria-label` prop to the `<InPageNavigation>` tag.
  Omit the term 'navigation'- it is redundant since the role is already defined as 'navigation'.
- To interact with In Page Navigation using the keyboard, use the tab key.
- Each In Page Navigation must have an In Page Navigation Item which is the currently selected page. To specify which page is current, add the `currentPage` prop to the respective `<InPageNavigationItem>`. Doing so will set `aria-current="page"` on that link.

## Examples

### Default In Page Navigation

<LivePreview
  scope={{
    InPageNavigation,
    InPageNavigationItem,
  }}
  noInline
>
  {basicExample}
</LivePreview>

### Full width In Page Navigation

Full width In Page Navigation allows the component to expand and fit the width of a containing element.

<LivePreview
  scope={{
    InPageNavigation,
    InPageNavigationItem,
  }}
  noInline
>
  {fullWidthExample}
</LivePreview>

---

## Usage Guide

### API

#### Installation

```bash
yarn add @twilio-paste/in-page-navigation - or - yarn add @twilio-paste/core
```

#### Usage

```jsx
import {InPageNavigation, InPageNavigationItem} from '@twilio-paste/core/in-page-navigation';

const InPageNavigationExample: React.FC = () => {
  return (
    <InPageNavigation aria-label="my-nav">
      <InPageNavigationItem href="#">Home</InPageNavigationItem>
      <InPageNavigationItem href="#" currentPage>
        About
      </InPageNavigationItem>
    </InPageNavigation>
  );
};
```

#### InPageNavigation Props

| Prop       | Type                                               | Description                                                                               | Default              |
| ---------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------- | -------------------- |
| aria-label | `string`                                           | Unique label for the InPageNavigation                                                     | null                 |
| children   | `ReactNode`                                        | InPageNavigationItem                                                                      | null                 |
| variant?   | `'fullWidth' \| 'inverse' \| 'inverse_fullWidth' ` | InPageNavigation variant                                                                  | null                 |
| element?   | `string`                                           | Overrides the default element name to apply unique styles with the Customization Provider | 'IN_PAGE_NAVIGATION' |

#### InPageNavigationItem Props

| Prop         | Type        | Description                                                                               | Default                   |
| ------------ | ----------- | ----------------------------------------------------------------------------------------- | ------------------------- |
| children     | `ReactNode` | Rendered text for the InPageNavigationItem                                                | null                      |
| currentPage? | `boolean`   | Sets the InPageNavigationItem as the current page and adds `aria-current="page"`          | false                     |
| href         | string      | A URL to route to. Required.                                                              | null                      |
| rel?         | any         | Sets the anchor rel attribute.                                                            | null                      |
| element?     | `string`    | Overrides the default element name to apply unique styles with the Customization Provider | 'IN_PAGE_NAVIGATION_ITEM' |

<ChangelogRevealer>
  <Changelog />
</ChangelogRevealer>

</content>

</contentwrapper>
